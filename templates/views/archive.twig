{% extends "base.twig" %}

{% block content %}

<div class="container">
	<h1 class="h1">{{ title }}</h1>

	<section class="posts posts--all flexrow flexrow--gutter">

		{% for post in posts %}
		<!-- {% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %} -->
		{% include "tile-post.twig" %}
		{% endfor %}

	</section>
</div>
{% endblock %}
